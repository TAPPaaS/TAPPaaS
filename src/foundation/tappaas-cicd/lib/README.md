# lib/

Shared libraries for tappaas-cicd managers/controllers (ADR-007 P4). Never copied per component.
Populated during S0 from `common-install-routines.sh`, `apply-json-merge.sh`, `audit-jq-readers.sh`.
