#!/usr/bin/env bash
# TEMPLATE/validate.sh — copy this directory to scaffold a new manager component.
# The parent dispatcher SKIPS TEMPLATE/, so this stub never runs in place.
set -euo pipefail
echo "[TEMPLATE] validate: schema/reference validation for this manager domain"
