{
  "description": "Test fixture: cluster:lxc provisioner + drift reconciler (issue #203). Disposable plain-Debian container (no GPU) used by ../test.sh --deep; safe to delete. zone0=srv-home (VLAN 210) — the only test VLAN this switch trunks cross-node.",
  "version": "0.1",
  "vmname": "test-lxcdrift",
  "vmid": 922,
  "vmtag": "TAPPaaS,Test",
  "node": "tappaas1",
  "cores": 1,
  "memory": "1024",
  "diskSize": "8G",
  "storage": "tanka1",
  "imageType": "clone",
  "image": "debian-12-standard_12.12-1_amd64.tar.zst",
  "bridge0": "lan",
  "zone0": "srv_home",
  "dependsOn": [
    "cluster:lxc"
  ]
}
