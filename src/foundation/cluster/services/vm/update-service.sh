#!/usr/bin/env bash
#
# TAPPaaS Cluster VM Service - Update
#
# Placeholder for VM update operations.
# Currently no action needed; VM updates are handled by the OS-level update scripts.
#
# Usage: update-service.sh <module-name>
#

set -euo pipefail

echo "cluster:vm update-service called for module: ${1:-unknown} (no action needed)"
exit 0
