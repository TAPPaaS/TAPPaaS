{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "site-fields.json",
  "title": "TAPPaaS Site",
  "description": "The Site is the top-level umbrella for a TAPPaaS installation (ADR-007d). It holds site-wide identity, location, hardware (Proxmox nodes + their storage pools), backup target, update schedule, module catalog repositories, and references to the organization config files. Environments are NOT listed here — they are simply whatever is present under config/environments/*.json (enumerated from the directory, not from site.json). Domain, DNS, and identity are per-environment (NOT site-wide) and live in config/environments/*.json. site.json replaces configuration.json at cutover; during the phased migration both files may coexist. See ADR-007 P2.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "defaultEnvironment",
    "displayName",
    "owner",
    "location",
    "hardware",
    "repositories"
  ],
  "properties": {
    "name": {
      "description": "Neutral site code (e.g. warmelo1 — place plus number). Aligned with the Proxmox cluster name at creation, informational afterwards (ADR-007d #426). Not used to name any environment, organization, or zone — those key on defaultEnvironment.",
      "type": "string",
      "pattern": "^[A-Za-z0-9_.-]+$"
    },
    "defaultEnvironment": {
      "description": "Name of the default (tenant) environment — also its network zone and owner organization. Decoupled from the site code so site.name can be renamed with a single edit without touching any environment/org/zone (ADR-007d #426). Must be a valid zone/environment slug.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "displayName": {
      "description": "Human-readable site name.",
      "type": "string",
      "minLength": 1
    },
    "owner": {
      "description": "Reference to the site-owner Organization.name (config/people/organizations/<owner>.json).",
      "type": "string"
    },
    "email": {
      "description": "Installer/admin contact email (Let's Encrypt ACME account, people bootstrap). Carried over from configuration.json .tappaas.email during the S3b reader cutover.",
      "type": "string"
    },
    "version": {
      "description": "Site config schema version.",
      "type": "string",
      "default": "1.0"
    },
    "location": {
      "description": "Physical/legal location of the site.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "country",
        "timezone"
      ],
      "properties": {
        "country": {
          "description": "ISO 3166-1 alpha-2 country code (e.g. NL).",
          "type": "string",
          "pattern": "^[A-Za-z]{2}$"
        },
        "timezone": {
          "description": "IANA timezone (e.g. Europe/Amsterdam).",
          "type": "string",
          "minLength": 1
        },
        "locale": {
          "description": "POSIX locale (e.g. nl_NL).",
          "type": "string",
          "default": "en_US"
        }
      }
    },
    "network": {
      "description": "Site WAN configuration. No rootDomain — domains are per-environment.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "isp": {
          "description": "ISP name, for operator reference only.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "publicIp": {
          "description": "Public IP address, or \"auto\" to discover at runtime.",
          "type": "string",
          "default": "auto"
        }
      }
    },
    "hardware": {
      "description": "Physical hardware: the Proxmox nodes that make up the cluster.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "nodes"
      ],
      "properties": {
        "nodes": {
          "description": "List of Proxmox nodes. nodeCount is computed from this array (no separate field).",
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "name"
            ],
            "properties": {
              "name": {
                "description": "Node name (e.g. tappaas1).",
                "type": "string",
                "pattern": "^[A-Za-z0-9_-]+$"
              },
              "storagePools": {
                "description": "Storage pools available on this node (e.g. [\"tanka1\", \"tankb1\"]).",
                "type": "array",
                "items": {
                  "type": "string"
                },
                "uniqueItems": true,
                "default": []
              }
            }
          }
        }
      }
    },
    "backup": {
      "description": "Site-wide backup target. null when no backup is configured yet.",
      "type": [
        "object",
        "null"
      ],
      "default": null,
      "additionalProperties": false,
      "properties": {
        "target": {
          "description": "Backup server hostname (Proxmox Backup Server).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "offsite": {
          "description": "Offsite backup buddy identifier.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "defaultRetention": {
          "description": "Site-wide default backup retention period (e.g. 7y, 5y, 1y). The base of the Site -> Environment -> Module retention cascade (ADR-007 P9): environments and modules may override it.",
          "type": "string",
          "default": "7y"
        },
        "offsiteResidency": {
          "description": "Data residency of the offsite backup buddy (ADR-007 P9). Used by 'backup-manager validate' to reject backing an eu-only environment up to a non-EU offsite. eu-only by default; set global only when the offsite is outside the EU.",
          "type": "string",
          "enum": [
            "eu-only",
            "global"
          ],
          "default": "eu-only"
        }
      }
    },
    "updateSchedule": {
      "description": "Site-wide update schedule [period, day, hour] (e.g. [\"monthly\", \"Thursday\", 2]).",
      "type": "array",
      "default": [
        "monthly",
        "Thursday",
        2
      ]
    },
    "automaticReboot": {
      "description": "Whether updates may reboot automatically. Site-wide until P3 (then moves to Environment).",
      "type": "boolean",
      "default": true
    },
    "snapshotRetention": {
      "description": "Number of snapshots to retain. Site-wide until P3 (then moves to Environment).",
      "type": "integer",
      "minimum": 0,
      "default": 5
    },
    "repositories": {
      "description": "Registry of checked-out repositories whose module catalogs back NAME-based module resolution (resolve-module.sh, ADR-004). This is NOT the only way a module is located and NOT required for a module to work: install-module.sh takes the current directory first, recording the module's source dir as .location in its deployed config, so a module installed from an unregistered path resolves through .location alone (issue #460).",
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "url"
        ],
        "properties": {
          "name": {
            "description": "Repository identifier.",
            "type": "string"
          },
          "url": {
            "description": "Repository URL.",
            "type": "string"
          },
          "branch": {
            "description": "Branch tracked by the local checkout.",
            "type": "string"
          },
          "path": {
            "description": "Absolute path of the local checkout. Load-bearing: resolve-module.sh skips any entry without it, so a repository lacking path contributes no modules to name-based resolution. Not in `required` only so site.json files written before it was declared still validate.",
            "type": "string"
          },
          "managed": {
            "description": "full = the repository carries TAPPaaS modules and its catalog is validated on add; tracked = registered without catalog requirements.",
            "type": "string",
            "enum": [
              "full",
              "tracked"
            ],
            "default": "full"
          },
          "catalog": {
            "description": "Module catalog path, relative to the repository root. Written by `repository add` as the path it actually found (which may be the legacy src/modules.json) and read by resolve-module.sh. Absent means: try src/module-catalog.json, then legacy src/modules.json.",
            "type": "string",
            "default": "src/module-catalog.json"
          },
          "updateChannel": {
            "description": "Update channel (stable | main | etc.).",
            "type": "string",
            "default": "stable"
          }
        }
      }
    },
    "organizations": {
      "description": "Paths to organization JSON files (config/people/organizations/*.json).",
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    }
  }
}
