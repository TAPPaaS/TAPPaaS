{
  "$schema": "service-fields.json",
  "scope": "module",
  "description": "Change semantics for the module-level fields that no provider service owns.",
  "_note": "ADR-020 D9 / issue #567. module-fields.json declares 74 fields; 55 name a '<module>:<service>' coordinate in their usedBy and are classified by that service's services/<svc>/fields.json. The 19 here name none — their usedBy is empty or 'general' — so they describe the MODULE itself (its provenance, lifecycle and wiring) rather than anything a provider configures, and until now nothing declared what changing them costs. This manifest is DECLARATION ONLY: no code consumes it yet. module-manager's pre-gate still waves every one of these through as 'config-only' without consulting a class, which is why setting kind, location or installTime by hand is currently accepted. Teaching the pre-gate to refuse on this manifest — the way network-manager already refuses a zone field with no changeClass in zones-fields.json — is issue #567, deliberately deferred until ADR-020 has landed across the estate. No entry carries a liveKey: there is no report-service.sh for module-level fields, because there is no provider to ask. Every entry is therefore apply:'none' — for these fields, writing the config IS the change. That makes 'in-place' here mean 'takes effect at once, nothing to refuse' rather than 'the converge applies it live'; converge.ts already calls this situation 'config-only', and #567 may be the moment that becomes a real class.",
  "fields": {
    "description": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Pushed to the Proxmox summary page when the guest is created and never pushed again, so an edit after install changes config only. Classed by what changing it COSTS (nothing, no disruption), not by today's gap — `qm set --description` works on a live VM, so a hook would make this a real `set` under cluster:vm."
    },
    "version": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "The module's own semver. Read to decide whether an upgrade exists; nothing on the cluster reflects it."
    },
    "appVersion": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Upstream application version, informational. No format enforced, so nothing can validate it beyond being a string."
    },
    "releaseDate": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Informational."
    },
    "maintainer": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Informational."
    },
    "status": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Cheap to change, consequential in effect: `archived` and `external` take the module OUT of the update lifecycle entirely — `module-manager module modify` skips it unless --force. Set automatically by `module-manager module delete --archive`."
    },
    "tier": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "foundation ⇒ mgmt-only, single-instance, --force to delete, and `source` must be official (or --allow-fork). Changing it changes what the PLATFORM will permit, never the guest."
    },
    "source": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Trust marker, normally inferred at deploy from the catalogue repository and pinned only when the module file must. A foundation module from a non-official source fails the tier/source lint."
    },
    "rebootOk": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "The ADR-020 D8 disruption authorization. Nothing APPLIES it — the converge READS it to decide whether an unattended pass may reboot or offline-migrate this guest. Default false, so silence never authorizes downtime."
    },
    "config": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "The Pattern-A container, not a value: normalized to the flat form at deploy and at validation, so it has no state of its own. Listed for coverage — every schema field is classified or the manifest is incomplete."
    },
    "dependsOn": {
      "class": "manual",
      "apply": "none",
      "changeNote": "install-service.sh and delete-service.sh run off the DELTA `module-manager module modify` computes across the 3-way merge (the union of dependsOn+integratesWith before vs after). A `--set` writes the DEPLOYED side, so before and after agree, the delta is empty, and no service script runs — the config then claims a dependency that was never wired, or drops one whose wiring is still in place. Change it in the SOURCE module file and run `module-manager module modify`."
    },
    "integratesWith": {
      "class": "manual",
      "apply": "none",
      "changeNote": "As dependsOn, but optional: a provider that is not installed is ignored rather than blocking, and its install auto-wires pre-existing integrators. Same delta mechanism, so the same caveat applies."
    },
    "provides": {
      "class": "manual",
      "apply": "none",
      "changeNote": "What OTHER modules may declare a dependency on. Removing an entry breaks every consumer's resolution, which is why this is not a free edit even though nothing local changes."
    },
    "environment": {
      "class": "immutable",
      "apply": "none",
      "changeNote": "Written by copy-update-json.sh --environment (ADR-007 P5). `module-manager module modify` and `module-manager module delete` resolve the SOURCE module file through it, so a wrong value points the lifecycle at the wrong module."
    },
    "moduleSource": {
      "class": "immutable",
      "apply": "none",
      "changeNote": "Written by copy-update-json.sh (was `location`, #609). get_module_dir() reads it as the SOLE source of the module directory and returns success for any path that exists — so a wrong value does not fail loudly, it silently runs the lifecycle against the wrong directory."
    },
    "location": {
      "class": "immutable",
      "apply": "none",
      "deprecated": true,
      "changeNote": "Written by copy-update-json.sh — the pre-#609 name of moduleSource, read until migration 0006 has run. get_module_dir() reads it as the SOLE source of the module directory and returns success for any path that exists — so a wrong value does not fail loudly, it silently runs the lifecycle against the wrong directory."
    },
    "kind": {
      "class": "immutable",
      "apply": "none",
      "changeNote": "Changing what type of workload a module is — a vm into an lxc — is a rebuild, not an edit, so `modify --set kind=…` is refused; change it in the module's source JSON. No provider reads it back from the live system, so the converge never compares it."
    },
    "installTime": {
      "class": "immutable",
      "apply": "none",
      "changeNote": "Stamped by copy-update-json.sh at install. Reporting only; never re-derived."
    },
    "updateTime": {
      "class": "immutable",
      "apply": "none",
      "changeNote": "Bumped by `module-manager module modify` on every successful update. Reporting only."
    },
    "variant": {
      "class": "immutable",
      "apply": "none",
      "changeNote": "RETIRED, superseded by `environment`. Never write it; installs strip it (copy-update-json.sh del(.variant)). Retained in module-fields.json only so a pre-migration config carrying it is classified rather than reported unknown."
    },
    "physicalLocation": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Records where a machine physically is (#609); changing it moves nothing, it only changes what the off-site check compares with the Site."
    },
    "stack": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Where a module is grouped (#463). Nothing on a deployed module reads it; moving a module between stacks is a repository change (and, after #421, a directory move — scripts/move-module.sh)."
    },
    "roles": {
      "class": "manual",
      "apply": "none",
      "changeNote": "What a machine is for (ADR-022h). Nothing re-provisions the machine when it changes: a satellite's roles are rendered onto it at install, so a new set takes `module-manager module delete <instance> --decommission` and an add. The `backup` role is set by `module modify <instance> --lockdown`, never by --set (ADR-010 §8.4)."
    },
    "address": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Where the mothership reaches a machine; changing it re-points TAPPaaS, it does not move the machine."
    },
    "management": {
      "class": "in-place",
      "apply": "none",
      "changeNote": "Takes a module in or out of TAPPaaS's lifecycle; nothing is applied to the module itself."
    }
  }
}
