#!/usr/bin/env bash
#
# TAPPaaS Proxy Service - Install
#
# Configures the reverse proxy (Caddy) for a consuming module.
#
# Usage: install-service.sh <module-name>
#

set -euo pipefail

echo "firewall:proxy install-service called for module: ${1:-unknown} (not yet implemented)"
exit 0
