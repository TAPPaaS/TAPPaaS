#!/usr/bin/env bash
#
# TAPPaaS Backup VM Service - Delete
#
# Removes backup configuration for a consuming module's VM.
#
# Usage: delete-service.sh <module-name>
#

set -euo pipefail

echo "backup:vm delete-service called for module: ${1:-unknown} (not yet implemented)"
exit 0
