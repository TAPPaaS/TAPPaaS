# Deprecated

Legacy files kept only for reference / one-time migration. **They are not part of
the live system and can be ignored** — nothing in the current install, update, or
test paths calls them. They are retained (rather than deleted) so an operator
migrating an older TAPPaaS installation can still see the previous behaviour.

Do not build new work on anything here.
