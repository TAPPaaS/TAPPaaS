# Authors

<!--
  OPTIONAL, per-module authorship record (#338). One line per copyright-bearing
  author: "<year> @<github-handle> — <role>". Additive: entries are appended,
  never rewritten. Role examples: original author, substantial rewrite.

  This file is NOT read by the platform. The machine-readable governance field
  is `maintainer` in <module>.json (used for automated issue routing).
  Contributors (anyone with an accepted change) are recorded by git history and
  are not listed here. See src/foundation/schemas/README.md for the role model,
  including the AI-assistance policy.
-->

- <year> @<handle> — original author
